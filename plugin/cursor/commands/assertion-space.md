Show my Assertion memory spaces, or switch this session to one. Run exactly this one command in
the terminal, adding the space name I typed after the command (if any) to the end, in quotes:

```
{{ASSERTION}} space --client cursor
```

Naming a space is my confirmation. Show me the output exactly as written. Don't add to it, don't
call the `use_space` or `list_spaces` tools, and don't run anything else.
