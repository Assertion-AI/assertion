Sign me in to Assertion memory. Run exactly this one command in the terminal and wait for it to
finish. It opens my browser, where I click Connect; it can take a minute or two:

```
{{ASSERTION}} login --client cursor
```

If I typed a word after the command (like `again` or `code`), add it to the end of that command.
Then show me its output exactly as written. Don't add to it, and don't run anything else.
