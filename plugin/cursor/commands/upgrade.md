Update the Assertion memory plugin to the latest version. Run exactly this one command in the
terminal and wait for it to finish:

```
{{ASSERTION}} upgrade --client cursor
```

It pulls the latest version into the clone Cursor runs the plugin from, and refreshes these
commands. Then show me its output exactly as written. Don't add to it, and don't run anything else.
