---
description: Catch up on a topic, decision, or where you left off — grounded in project memory
argument-hint: [node id like n6627, a topic, or blank for where you left off]
allowed-tools: mcp__plugin_assertion_assertion__recall, mcp__plugin_assertion_assertion__expand
---

Catch me up on: **$ARGUMENTS**

If the line above is blank, catch me up on wherever I left off — the most
recently-active thread in this project's memory.

Ground every claim in the project memory — do not guess or infer:

1. Use `recall` on the topic/keywords above to find the relevant stored
   decisions and findings, and `expand` the node ids you turn up — including any
   id given to me directly (e.g. `n6627`). Pull the actual current state, not
   your recollection.
2. If no topic was given, identify the most recently-touched thread (from the
   working set already in context, or via `recall`) and catch me up on that.

Then write the catch-up in this structure, citing node ids `[nXXXX]` throughout
so I can verify each point and drill in:

- **The goal** — what this thread/initiative is trying to achieve (1–2 sentences).
- **Where it landed** — the key decisions and their current state. If a decision
  was changed or superseded, show both the old and the new and cite each.
- **Watch-outs** — open questions, risks, and "if X then Y" gotchas that matter
  for picking this back up.
- Close by offering to pull the full thread on any one sub-part.

Keep it skimmable. Cite real node ids only — never invent one. If the memory
genuinely doesn't cover something, say so rather than filling the gap.
